{
  "manifest_version": 3,
  "name": "tb bridge",
  "version": "1.0.0",
  "description": "Lets the tb CLI drive this Chrome profile — your tabs, your logins, no restart.",
  "permissions": ["debugger", "tabs", "alarms", "identity", "identity.email"],
  "host_permissions": ["<all_urls>"],
  "background": {
    "service_worker": "background.js"
  }
}
